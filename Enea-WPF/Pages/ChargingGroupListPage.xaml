<UserControl x:Class="Enea_WPF.ChargingGroupListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Charger Group list" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="10" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Create Charger Group" Width="150" Margin="0,0,10,0" Click="CreateUserButton_Click" />
            <Button Content="Edit Charger Group" Width="150" Margin="0,0,10,0" Click="EditUserButton_Click" />
            <Button Content="Delete Charger Group" Width="150" Click="DeleteUserButton_Click" />
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="dataGridUsers" AutoGenerateColumns="True"
                  AutoGeneratedColumns="DataGridUsers_OnAutoGeneratedColumns"
                  ItemsSource="{Binding Items}" Margin="10" IsReadOnly="True" />
    </Grid>
</UserControl>